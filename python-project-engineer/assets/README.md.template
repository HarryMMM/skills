# {{PROJECT_NAME}}

## 项目简介
{{PROJECT_DESCRIPTION}}

## 运行环境
- Python {{PYTHON_VERSION}}

## 安装依赖
```bash
pip install -e .
```

## 开发环境
```bash
pip install -e ".[dev]"
```

## 启动
```bash
python -m app.main
```

## 测试数据
项目默认使用内存 SQLite，启动时自动建表并加载 3 条测试数据，无需手动初始化。

如需重新初始化，可运行：`python scripts/seed_data.py`

## 切换到 MySQL
1. 安装 MySQL 驱动：`pip install -e ".[mysql]"`
2. 在 `.env` 中设置：`DB_URL=mysql+aiomysql://user:password@host:port/db_name`
