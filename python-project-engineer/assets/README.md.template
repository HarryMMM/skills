# {{PROJECT_NAME}}

## 项目简介
{{PROJECT_DESCRIPTION}}

## 运行环境
- Python {{PYTHON_VERSION}}

## 安装依赖
```bash
pip install -e .
```

## 开发环境
```bash
pip install -e ".[dev]"
```

## 启动
```bash
python -m app.main
```

启动后将作为 MCP Server 运行，通过 stdio 传输协议提供工具调用。

## MCP 示例工具
项目内置了一个查询 `items` 表的 MCP 示例工具，使用前需要：
1. 在 `.env` 中配置真实数据库连接信息
2. 在数据库中创建 `items` 表：
```sql
CREATE TABLE items (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  description TEXT
);
```
